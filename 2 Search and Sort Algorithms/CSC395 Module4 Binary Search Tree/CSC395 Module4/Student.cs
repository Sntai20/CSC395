using System;
namespace CSC395_Module4
{
    // Implement a Student class that stores name, major, and 
    //state of origin. The students should be sorted alphabetically by names in the tree:
    public class Student
    {
        // Student Data

        // Student Constructor
        public Student()
        {
        }

        // Methods
        //[20 points] Insert(a new value into the tree while maintaining the BST structure): void insert(str studName, str major, str originState)
        public void Insert(string studName, string major, string originState)
        {
            throw new Exception("Not implemented");
        }
        //[20 points] Insert(a new value into the tree while maintaining the BST structure): void insert(Student newStudent)
        public void Insert(Student newStudent)
        {
            throw new Exception("Not implemented");
        }
    }
}
